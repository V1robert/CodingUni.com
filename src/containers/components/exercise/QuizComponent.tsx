import type {Exercise} from "../../../types/types.ts";

interface QuizComponentProps {
    exercise?: Exercise
}

// eslint-disable-next-line no-empty-pattern
const QuizComponent = ({}: QuizComponentProps) => {
    return (
        <div>

        </div>
    );
};

export default QuizComponent;